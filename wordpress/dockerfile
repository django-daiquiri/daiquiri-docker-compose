FROM wordpress

RUN apt-get update && \ 
       apt-get install -y git

# RUN mkdir -p /var/www/html/wp-content/plugins/ &&\
#      mkdir -p /var/www/html/wp-content/theme/ &&\
#      git clone https://github.com/django-daiquiri/wordpress-plugin /var/www/html/wp-content/plugins/daiquiri && \
#      git clone https://github.com/django-daiquiri/wordpress-theme /var/www/html/wp-content/themes/daiquiri

COPY ./rootfs /

CMD ["/drun.sh"]
