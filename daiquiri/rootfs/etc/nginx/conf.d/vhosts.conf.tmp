# in /etc/nginx/conf.d/vhost.conf        on RHEL/CentOS
server {
    listen 80;
    server_name YOURDOMAIN;

    location / {
        proxy_pass http://unix:/var/run/daiquiri.sock;
    }
    location /static/ {
        alias /vol/daiquiri/<DAIQUIRI_APP>/static_root/;
    }
}
